[tox]
skipsdist = True
envlist = py34, py37, py38, pep8

[testenv]
commands =
    pip install -e .
    pip install -r test-requirements.txt
    py.test {posargs}

passenv =
       LANG

[testenv:pep8]
commands =
    pip install flake8
    flake8 pycobertura/
